{
  "mcp": {
    "servers": {
      "memograph": {
        "command": "python",
        "args": [
          "-m",
          "memograph.mcp.run_server"
        ],
        "env": {
          "MEMOGRAPH_VAULT": "C:\\Users\\YOUR_USERNAME\\Documents\\my-vault"
        },
        "comment": "LLM provider is optional. If not specified, query_with_context returns context for Cline's LLM to use. To enable answer generation in MemoGraph, add: MEMOGRAPH_PROVIDER=ollama and MEMOGRAPH_MODEL=llama3.1:latest"
      }
    }
  }
}
